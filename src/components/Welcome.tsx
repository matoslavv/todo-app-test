import { Typography } from "@mui/material";

const Welcome = () => {
  return (
    <Typography variant="h4" component="h2" align="left" pr={3} mt={4}>
      Welcome
    </Typography>
  );
};

export default Welcome;
